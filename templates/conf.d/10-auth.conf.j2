disable_plaintext_auth = yes
auth_verbose = yes
auth_mechanisms = plain
passdb {
	driver = passwd-file
	args = /etc/dovecot/passwd
}
userdb {
	driver = static
	args = uid={{ mailbox_userid }} gid={{ mailbox_groupid }} home=/home/{{ mailbox_userid }}/%d/%n
}
